name: tests

on:
  push:
  pull_request:
  workflow_dispatch:   # manual run button; delete this line if unwanted

permissions:
  contents: read       # least privilege

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: make test
